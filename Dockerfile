# Используем JDK как базовый образ
FROM openjdk:latest

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем все файлы из текущей директории (включая исходный код) в контейнер
COPY . /app

# Компилируем исходный код Java
RUN javac -cp junit.jar:. *.java

# Команда для запуска приложения (если нужно)
# CMD ["java", "MainClass"]
